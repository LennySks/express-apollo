type User {
    id: ID!
    username: String!
    email: String!
    task: [Post!]!
}

type Query {
    users: [User!]!
    user(id: ID!): User
    posts: [Post!]!
    post(id: ID!): Post
}

type Post {
    id :ID!
    title: String
    content: String
    authorId: String
    addedAt: String
    user: User
}